{listen, [
  {{4560, "fc00::63"}, ejabberd_xmlrpc, [{maxsessions, 10}, {timeout, 5000},
    {access_commands, [
      {xmlrpc, [register, set_last, check_account, change_password, send_message, registered_users, unregister], []}
    ]}
  ]}
]}.
