{listen, [
  {{4560, "fd00::63"}, ejabberd_xmlrpc, [{maxsessions, 10}, {timeout, 5000},
    {access_commands, [
      {xmlrpc, [register, set_last, check_account, check_password, change_password, send_message, registered_users, unregister], []}
    ]}
  ]}
]}.
